

#include <stdio.h>

int main()
{
    int i, start;
    // printf("Enter starting value: ");
    // scanf("%d", &start);
     start=10;
    
    for(i=start; i>=1; i--)
    {
        printf("%d\n", i);
    }

    return 0;
}